# Thoughts about persistence and storing context
I think that it would be ideal to store all history of chatter with the bot, and have a base character card that is unmodified stored so that if context management strategies change in the future, no data is lost.
